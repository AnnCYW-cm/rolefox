# Security Policy

RoleFox 会在浏览器本地处理用户放入的岗位、规则和判断记录，因此本地数据保护、导入校验和导出边界属于产品核心，而不是附加功能。当前 Alpha 不读取浏览器会话，也不执行投递、消息或其他外部操作。

## 支持版本

| 版本 | 安全更新 | 实际能力边界 |
| --- | --- | --- |
| `0.1.0-alpha.7` | ✅ | Clear Signal 浏览器本地岗位判断 Alpha；包括 CSV/JSON 预览导入、重复检查、岗位编辑、搜索与六种筛选、感兴趣清单 JSON 导出及 15 MB 写入保护 |
| `0.1.0-alpha.6` | ❌ | 已由 `0.1.0-alpha.7` 取代；作为 Fox Ledger 历史版本保持不可变 |
| `0.1.0-alpha.5` | ❌ | 已由 `0.1.0-alpha.6` 取代；作为已发布的历史版本保持不可变 |
| `0.1.0-alpha.4` | ❌ | 已由后续版本取代；作为已发布的历史版本保持不可变 |
| `0.1.0-alpha.3` | ❌ | 已由后续版本取代；作为已发布的历史版本保持不可变 |
| `0.1.0-alpha.2` | ❌ | 已由后续版本取代；作为已发布的历史版本保持不可变 |
| `0.1.0-alpha.1` | ❌ | 首次公开 Alpha 的历史记录仍保留 |
| 更早的 pre-alpha 快照 | ❌ | 不再提供安全更新 |

`0.1.0-alpha.7` 沿用 `0.1.0-alpha.6` 的确定性评分、版本化数据 schema、存储键与无外部动作安全边界，同时发布 Clear Signal / 明亮信号工作台和新的本地复核能力。用户可以先在当前浏览器预览 CSV/JSON 岗位文件，再确认写入；精确重复会被拦截，疑似重复会明确提示；已有岗位可编辑并重新评分；结果支持搜索以及全部、待决定、推荐、感兴趣、不感兴趣和已排除六种筛选；“感兴趣清单”由用户主动导出为包含岗位原文和当前判断依据的 JSON。岗位导入文件、备份恢复文件和序列化本地状态均有 15 MB 安全上限，超限时拒绝读取或写入而不覆盖已有数据。规则、岗位和校准选择仍保存在当前浏览器的 `localStorage` 中；文件不会上传。Alpha.1–Alpha.6 数据无需迁移，Alpha.6 的标签、版本说明和发布记录不会被 Alpha.7 追溯改写。

Alpha.7 没有账号、服务端存储、云同步、后台扫描、招聘平台连接器、AI 调用或真实投递/回复能力。它不构成用户研究证据，Pre-W1 Gate 1 继续保持 `BLOCKED_NOT_STARTED`，但该 Gate 不阻止这一窄范围开源 Alpha 发布。请把任何突破该边界的行为（例如意外网络传输、本地数据跨来源泄露、超限写入覆盖已有数据，或导出内容与界面说明不符）作为安全问题报告。

Alpha.6 的历史安全说明保留如下：

`0.1.0-alpha.6` 沿用 `0.1.0-alpha.5` 的数据 schema、存储键与安全边界：没有账号、服务端存储、云同步、后台扫描、招聘平台连接器或真实投递/回复能力。Fox Ledger 的暖纸色、墨黑、狐狸橙、连续结果台账、编辑栏与响应式导航只改变界面呈现。规则、手工录入的岗位和校准选择仍保存在当前浏览器的 `localStorage` 中；完整导出仍由用户主动下载，Alpha.1–Alpha.5 数据无需迁移。Alpha.5 的发布记录不会被 Alpha.6 追溯改写。请把任何突破该边界的行为（例如意外网络传输、本地数据跨来源泄露或导出内容与界面说明不符）作为安全问题报告。

## 报告漏洞

请优先使用 GitHub 的 **Private Vulnerability Reporting** 私下报告。请勿在公开 Issue 中粘贴密钥、Cookie、真实简历、聊天记录或可识别个人身份的信息。

报告中请包含：

- 受影响版本或提交
- 可复现步骤与影响范围
- 已脱敏的日志或最小示例
- 如已知，建议的缓解措施

维护者目标是在 7 天内确认收到报告，并在完成初步评估后同步修复计划。

## 高风险问题

- 账号、Cookie、令牌或简历数据泄露
- 未经有效委托策略授权或人工批准执行投递或发送消息
- 提示注入导致越权操作
- 操作限额、kill switch、授权判断或人工审批流程被绕过
- 招聘方或求职者个人信息泄露

当前发布是功能受限的浏览器本地 Alpha。请勿输入账号密码、Cookie、访问令牌或其他凭证，也不要将它描述或用于无人值守的真实投递。岗位正文可能包含敏感信息；使用共享设备时请在结束前导出所需数据并清除浏览器本地记录。
